---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="7ofu">
  <section class="hero">
    <h1>7ofu</h1>
    <p class="lead">
      アプリを作っています。ここは作ったものと、そのヘルプ・規約を置く場所です。
    </p>
  </section>

  <section>
    <h2>つくったもの</h2>
    <ul class="cards">
      <li class="card">
        <a href="/koura-diary/">
          <img src="/koura-diary.svg" alt="" class="card-icon" width="48" height="48" />
          <span class="card-body">
            <strong>こうら日記</strong>
            <small>リクガメの飼育記録アプリ。規約・プライバシー・免責はこちら。</small>
          </span>
        </a>
      </li>
      <li class="card">
        <a href="/schemely/">
          <img src="/schemely.svg" alt="" class="card-icon" width="48" height="48" />
          <span class="card-body">
            <strong>Schemely <span class="badge">準備中</span></strong>
            <small>Deep Link（URL スキーム / Universal Link）の挙動を確かめる検証ツール。規約・プライバシーはこちら。</small>
          </span>
        </a>
      </li>
    </ul>
  </section>

  <style>
    .hero { padding-block: 1rem 2.5rem; }
    .lead { color: var(--muted); font-size: 1.05rem; }
    .cards { list-style: none; padding: 0; margin: 0; display: grid; gap: 1rem; }
    .card {
      border: 1px solid var(--line);
      border-radius: 16px;
      background: white;
      transition: border-color 0.15s ease;
    }
    .card a {
      display: flex;
      gap: 1.15rem;
      align-items: center;
      padding: 1.35rem 1.5rem;
      text-decoration: none;
      color: var(--fg);
    }
    .card:hover { border-color: var(--brand); }
    .card-icon { flex: none; border-radius: 11px; }
    .card-body { display: flex; flex-direction: column; gap: 0.25rem; }
    .card-body small { color: var(--muted); }
    .badge {
      font-size: 0.7rem;
      font-weight: 600;
      color: var(--muted);
      background: #f0ebdd;
      border: 1px solid var(--line);
      border-radius: 999px;
      padding: 0.1rem 0.5rem;
      vertical-align: middle;
      margin-left: 0.35rem;
    }
  </style>
</BaseLayout>
